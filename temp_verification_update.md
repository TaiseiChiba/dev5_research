# 取引検証画面から最終確認画面への遷移方法

## 現在の実装状況

1. TransactionVerification.tsx に以下を追加済み：
   - `verifiedTransactions` 状態
   - `activeTab` 状態
   - `handleFinalConfirmation` 関数
   - 必要なインポート（Tabs, Tab, CheckCircleIcon）

## 次のステップ

1. 検証済みタブの表示機能を追加
2. 検証済み取引から最終確認画面への遷移ボタンを追加

## 使用方法

1. 取引検証画面で「検証済み」タブをクリック
2. 検証済み取引の「最終確認」ボタンをクリック
3. 最終確認画面に遷移してデータが表示される

## テスト用データ

現在はモックデータを使用（検証待ち取引の最初の2件を検証済みとして表示）
